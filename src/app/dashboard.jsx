import { StyleSheet, View } from "react-native";
function dashborad() {
  return <View style={styles.mainBox}></View>;
}

export default dashborad;

const styles = StyleSheet.create({
  mainBox: {
    flex: 1,
    backgroundColor: "#1A394B",
  },
});
